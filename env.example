# =============================================================================
# MemmoryServer V2 환경변수 설정 예시
# =============================================================================

# Qdrant 벡터 데이터베이스 설정
# -----------------------------------------------------------------------------
# Qdrant 서버 호스트 (Docker 환경에서는 'qdrant', 로컬에서는 'localhost')
QDRANT_HOST=localhost

# Qdrant 서버 포트 (기본값: 6333)
QDRANT_PORT=6333

# 서버 설정
# -----------------------------------------------------------------------------
# Memory Server 포트 (기본값: 5602)
SERVER_PORT=5602

# Memory Server 호스트 (기본값: 0.0.0.0)
SERVER_HOST=0.0.0.0

# OpenAI 임베딩 설정 (V2에서는 OpenAI만 지원)
# -----------------------------------------------------------------------------
# OpenAI API 키 (필수)
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI 임베딩 모델명 (기본값: text-embedding-3-small)
OPENAI_MODEL_NAME=text-embedding-3-small

# OpenAI 임베딩 벡터 차원 (기본값: 1536)
OPENAI_VECTOR_DIMENSION=1536

# OpenAI 사용자 식별자 (선택사항, 모니터링용)
OPENAI_USER_IDENTIFIER=memory_server_v2

# 컬렉션 벡터 차원 설정
# -----------------------------------------------------------------------------
# Episodic 메모리 벡터 차원 (기본값: 1536)
EPISODIC_VECTOR_DIM=1536

# Semantic 메모리 벡터 차원 (기본값: 1536)
SEMANTIC_VECTOR_DIM=1536

# 로깅 설정
# -----------------------------------------------------------------------------
# 로그 레벨 (기본값: INFO)
LOG_LEVEL=INFO

# 로그 파일 경로 (선택사항, 비워두면 파일 로깅 비활성화)
LOG_FILE=

# =============================================================================
# 사용 방법:
# 1. 이 파일을 .env로 복사: cp env.example .env
# 2. 필요한 값들을 실제 값으로 수정
# 3. .env 파일은 .gitignore에 포함되어 있어야 함
# ============================================================================= 